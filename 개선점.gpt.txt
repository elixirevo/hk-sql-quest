아래는 “정답 가능성/의도 충족” 관점에서 **핵심 논리 체크 + 자주 나는 함정(정수 나눗셈, 분모 0, 조건 방향, 집계 단위)** 위주로 검토한 내용이야. (DB가 MySQL 계열인지, PostgreSQL인지에 따라 `LIMIT`, 정수 나눗셈 동작이 달라질 수 있는데, 공통적으로 문제될 부분은 짚어둘게.)

---

## 공통으로 꼭 점검할 것 (전 문제에 영향)

1. **정수 나눗셈 위험**

* `MALE_CNT / (MALE_CNT + FEMALE_CNT)` 처럼 둘 다 정수면 DB에 따라 **0/1로 떨어질 수 있음**.
* 안전하게 `* 1.0` 또는 `CAST(... AS DECIMAL)`/`::numeric`로 실수화 권장.

2. **CASE WHEN ... THEN POPLTN_CNT END** 는 ELSE가 없으면 NULL

* `SUM(NULL)`은 0이 아니라 NULL이 될 수 있음(엔진/표현에 따라).
* 비율 계산에서 NULL이 전파될 수 있으니 `ELSE 0` 넣는 습관 추천.

3. **분모 0/NULL 처리**

* `NULLIF(전체, 0)` 사용은 좋음.
* 다만 `MALE_POP / FEMALE_POP` 같이 **여성=0**이면 바로 에러/NULL 가능 → 반드시 `NULLIF(FEMALE_POP,0)` 형태가 안전.

---

## 문제 1 검토: “연령대별 성비 편차(남성비율-여성비율) 0.1 이상” 연령대가 가장 많은 읍/면/동 TOP5

### 👍 좋은 점

* 읍면동 필터 (`ADMINIST_ZONE_NO NOT LIKE '_____00000'`) 의도는 맞아 보임.
* M/F만 집계해서 연령대별 남녀 합을 만든 뒤 비율 계산한 구조도 좋음.

### ⚠️ 수정/개선 포인트

1. **조건이 ‘DIFF >= 0.1’만 있음**

* 문제는 “편차가 큰(남성비율-여성비율)”인데, 문장 그대로면 남성이 더 큰 경우만 잡힘.
* 보통 성비 편차는 양/음 모두 큰 걸 의미하니까 **`ABS(DIFF) >= 0.1`**가 더 자연스럽고 출제 의도에도 부합할 가능성이 큼.

  * 만약 진짜로 “남성비율-여성비율이 0.1 이상(=남성이 10%p 이상 높음)”만 원했다면 지금이 맞고, 그 경우 주석에 “남성이 더 많은”을 명시하면 좋아.

2. **정수 나눗셈 가능성**

* `MALE_CNT / (MALE_CNT + FEMALE_CNT)`가 정수 나눗셈이면 대부분 0이 나와서 DIFF가 0이 되어 결과가 틀어짐.
* `MALE_CNT * 1.0 / NULLIF(MALE_CNT + FEMALE_CNT, 0)` 처럼 바꾸는 게 안전.

3. **MALE_CNT/FEMALE_CNT가 NULL일 수 있음**

* PIVOT에서 `SUM(CASE WHEN ... THEN POP_CNT END)`는 해당 성별이 없으면 NULL.
* 계산식에서 NULL 전파 가능 → `ELSE 0` 또는 `COALESCE(MALE_CNT,0)`.

---

## 문제 2 검토: “시/도별 청년층 비율 vs 노년층 비율, 청년층이 2배 이상 높은 시/도”

### 👍 좋은 점

* `POPLTN_SE_CD='T'`로 총인구 기준 집계한 건 정확한 방향.
* 시/도 필터 `ADMINIST_ZONE_NO LIKE '__00000000'`도 통상 시도 레벨을 잘 잡음.

### ⚠️ 수정/개선 포인트

1. **중복 쿼리**

* “(2배 이상 높은 시/도)”라고 해놓고 똑같이 `ORDER BY ADMINIST_ZONE_NO`로 끝나는 쿼리가 한 번 더 있음 → 불필요.

2. **2배 조건은 굳이 비율로 안 해도 됨**

* 네가 마지막 쿼리에서 비율 비교를 했는데,

  * `(청년/전체) >= 2*(노년/전체)` 는 `청년 >= 2*노년` 과 동일이야(전체>0이면).
* 마지막 WHERE를 더 간단히 `WHERE 청년층인구 >= 2 * 노년층인구`로 써도 의미 동일하고 더 안전/명확.

3. **CASE SUM에 ELSE 0 권장**

* `SUM(CASE WHEN ... THEN POPLTN_CNT END)`는 조건 미충족 시 NULL일 수 있어 엔진에 따라 결과가 NULL로 튈 수 있음 → `ELSE 0`.

---

## 문제 3 검토: “연령대별로 전국 대비 집중된 읍/면/동 TOP3”

문제 설명을 보면 보통 의도는:

* 각 읍면동의 “해당 연령대 비중” = (읍면동의 해당 연령대 인구 / 읍면동 전체 인구)
* 전국의 “해당 연령대 비중” = (전국 해당 연령대 인구 / 전국 전체 인구)
* **집중도** = (읍면동 연령대 비중) / (전국 연령대 비중)
* 이 집중도가 큰 읍면동을 연령대별 TOP3

### ⚠️ 현재 쿼리의 핵심 문제 (의도 불일치 가능성 매우 큼)

* 지금 `RATE = lp.LOCAL_TOTAL_POP / kp.CITY_TOTAL_POP` 는

  * “전국 해당 연령대 인구 중에서 이 읍면동이 차지하는 점유율”임.
* 이건 “집중도(지역 내 비중 대비 전국 평균)”가 아니라 **점유율 TOP3**라서,

  * 인구가 큰 동(예: 대도시 큰 행정동)만 계속 상위에 뜨는 구조가 돼.
* 또한 최종 SELECT가

  * 연령대별로 RNK<=3 뽑아놓고,
  * 다시 전체 RATE 기준 `REAL_RANK`로 섞어서 `LIMIT 3` 해버려서
  * **연령대별 TOP3**라는 요구사항을 깨고 “전체에서 RATE 큰 것 3개”만 남길 가능성이 큼.

### ✅ 방향 제안 (논리만)

* 연령대별로:

  1. 읍면동별 해당 연령대 인구 / 읍면동 전체 인구 = local_age_share
  2. 전국 해당 연령대 인구 / 전국 전체 인구 = national_age_share
  3. 집중도 = local_age_share / national_age_share
  4. `RANK() OVER (PARTITION BY AGRDE_SE_CD ORDER BY 집중도 DESC)` <= 3

이 형태로 바꾸면 문제 문구 “읍면동 연령대 인구 비율 / 전국 연령대 인구 비율”에 더 정확히 부합해.

---

## 문제 4 검토: “읍/면/동별 10대 비율, 60대 비율… 10대 비율이 60대 비율의 3배 이상”

### 👍 좋은 점

* 지역 총인구(`REGION_POP`)를 먼저 구하고, 10대/60대만 pivot한 다음 비교하는 흐름은 좋음.

### ⚠️ 수정/개선 포인트

1. **총인구 집계에서 성별 필터 없음**

* `REGION_POP`은 `POPLTN_SE_CD` 필터가 없어서 M/F/T가 모두 들어있다면 **중복합산** 위험이 큼.
* 문제는 “총 인구 대비 비율”이므로 보통 `POPLTN_SE_CD='T'`로 총인구를 잡아야 안전함.
* `AGE_POP`도 마찬가지로 `POPLTN_SE_CD='T'` 권장.

2. **정수 나눗셈**

* `TEEN_POP / TOT_POP` 정수 나눗셈이면 0 가능 → 실수화 필요.

3. **조건은 결국 TEEN_POP >= 3*SIXTY_POP 과 같음(전체>0)**

* 성능/명확성 측면에선 인구수로 비교하는 게 더 직관적.

---

## 문제 5 검토: “남성이 여성보다 50% 이상 많은 연령대 또는 여성이 남성보다 50% 이상 많은 연령대가 ‘가장 많은’ 지역 TOP10 (인구수)”

### ⚠️ 현재 쿼리는 문제 요구와 다름

* 지금은 조건을 만족하는 **연령대-지역 행**을 뽑고 `TOTAL_DIFF`(남녀 차이 인구수) 큰 것 TOP10을 뽑고 있음.
* 하지만 문제는:

  * “그런 연령대(조건 만족)가 **가장 많은 지역** TOP10”
  * 즉, 지역별로 조건 만족하는 연령대의 **개수 COUNT**로 랭킹해야 함.

### 또한 치명적인 안전 이슈

* `MALE_POP / FEMALE_POP >= 1.5` 에서 FEMALE_POP이 0이면 문제.
* 반드시 `MALE_POP / NULLIF(FEMALE_POP,0) >= 1.5` 식으로.

### 올바른 집계 방향(논리)

1. 지역+연령대별 MALE_POP, FEMALE_POP
2. `CASE WHEN MALE>=1.5*FEMALE OR FEMALE>=1.5*MALE THEN 1 ELSE 0 END` 를 “불균형 플래그”로 만들고
3. 지역별 `SUM(불균형 플래그)` 로 “불균형 연령대 개수” 계산
4. 그 개수 기준 TOP10

(“인구수”라는 괄호가 **차이 인구수**를 의미하는지 애매하지만, 문장상은 “연령대 수가 가장 많은 지역”이라 COUNT가 1순위고, 필요하면 tie-breaker로 총 차이 인구수 합 같은 걸 추가하는 식이 자연스러워.)

---

## 총평 (정답 가능성 기준)

* **문제 1, 2**: 큰 구조는 맞는데 **정수 나눗셈/ABS 처리/NULL 처리**만 고치면 매우 안정적.
* **문제 3**: 현재는 “집중도”가 아니라 “점유율”에 가까워서 **출제 의도와 어긋날 가능성 큼** + 마지막 `LIMIT 3` 로 연령대별 TOP3가 깨짐.
* **문제 4**: 총인구/연령대 인구를 `T`로 제한하지 않으면 **중복합산** 위험. 그 부분만 고치면 좋음.
* **문제 5**: 현재 결과는 “차이가 큰 연령대-지역 TOP10”이고, 요구는 “불균형 연령대 개수가 많은 지역 TOP10”이라 **집계 목표가 다름**.

원하면, DB가 MySQL/PostgreSQL/BigQuery 중 뭔지 가정해서(지금 쿼리 문법은 MySQL/PG 섞여도 대체로 가능) **각 문제별로 정답형 쿼리를 깔끔하게 다시 써줄게.**
